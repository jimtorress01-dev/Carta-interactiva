<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Carta interactiva — Eterna</title>
  <style>
    :root{
      --bg:#0f1720; /* dark */
      --card:#f0fff4; /* very light green */
      --accent:#a8d5ba; /* pastel green */
      --dark:#0b0f12;
      --glass: rgba(255,255,255,0.06);
      --text:#0b0f12;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;color:var(--text);background:radial-gradient(1200px 600px at 10% 10%, rgba(168,213,186,0.06), transparent), linear-gradient(180deg, #071019 0%, #0f1720 100%);display:flex;align-items:center;justify-content:center}

    .scene{width:360px;max-width:92vw;perspective:1200px}
    .card{
      width:100%;height:520px;border-radius:22px;position:relative;padding:28px;background:linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.02));box-shadow:0 8px 30px rgba(2,6,10,0.6), inset 0 1px 0 rgba(255,255,255,0.02);backdrop-filter:blur(6px);overflow:hidden;
      transition:transform .9s cubic-bezier(.2,.9,.2,1), height .6s ease;
    }

    /* portada */
    .front{
      position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:18px;z-index:4;transform-origin:center;transition:transform .9s cubic-bezier(.2,.9,.2,1), opacity .45s ease;
    }

    .logo{font-weight:700;letter-spacing:1px;color:var(--accent);font-size:20px}
    .subtitle{font-size:13px;color:rgba(255,255,255,0.7)}

    .heart-wrap{width:160px;height:160px;border-radius:18px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));display:flex;align-items:center;justify-content:center;box-shadow:0 6px 20px rgba(10,14,16,0.6);cursor:pointer;transition:transform .18s ease}
    .heart{width:92px;height:92px;display:inline-block;transform-origin:center;transition:transform .18s ease}

    /* corazón SVG fill */
    .heart svg{width:100%;height:100%;filter:drop-shadow(0 6px 12px rgba(0,0,0,0.35));}

    /* estado abierto */
    .card.open{height:640px}
    .card.open .front{transform:translateY(-40px) scale(.98) rotateX(18deg);opacity:0.03}

    /* interior */
    .inner{position:absolute;left:0;right:0;bottom:0;top:0;padding:44px;display:flex;flex-direction:column;gap:14px;align-items:center;justify-content:flex-start;z-index:2;opacity:0;transform:translateY(28px);transition:opacity .6s ease .15s, transform .6s ease .15s}
    .card.open .inner{opacity:1;transform:translateY(0)}

    .content-box{width:100%;max-width:88%;border-radius:14px;padding:18px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));box-shadow:0 8px 30px rgba(5,8,10,0.7);backdrop-filter: blur(6px)}
    .message{font-size:18px;line-height:1.45;color:rgba(255,255,255,0.95);min-height:120px}

    .instructions{font-size:13px;color:rgba(255,255,255,0.6);margin-top:8px}

    /* pequeño ornamento abajo */
    .footer{position:absolute;left:18px;right:18px;bottom:14px;display:flex;justify-content:space-between;align-items:center;color:rgba(255,255,255,0.45);font-size:12px}
